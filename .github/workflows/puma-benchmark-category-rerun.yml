name: PUMA Benchmark 100 Resumable Status

on:
  push:
    branches: [puma-benchmark-category-rerun]
    paths:
      - '.github/workflows/puma-benchmark-category-rerun.yml'

jobs:
  status:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Show benchmark status
        run: |
          set -euo pipefail
          curl -fsS 'https://rizweb-0-2.onrender.com/api/jobs/08aa3073db27'
# automation-status-probe-6
